// Package workspace is the root of the V3R1C0R3 monorepo.
// It contains no code; the workspace exists so that go build ./...
// from the repo root builds all modules (apps/api, packages/db, packages/mcp-flight-recorder).
package workspace
